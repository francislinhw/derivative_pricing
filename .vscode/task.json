{
    "tasks": [
        {
            "label": "C/C++: Build FittedBondCurve",
            "type": "shell",
            "command": "/usr/bin/g++",  // or the correct path to your g++ compiler
            "args": [
                "`quantlib-config --cflags`",// Adjust the path to FittedBondCurve.cpp
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "`quantlib-config --libs`"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Build generated by the debugger."
        }
    ]
}
