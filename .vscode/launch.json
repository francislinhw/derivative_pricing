{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C/C++ Runner: Debug Session",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/bin",
      "args": [],
      "cwd": "${fileDirname}",
      "preLaunchTask": "C/C++: Build with QuantLib"
    }
  ]
}